{
  "name": "@dpgc/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Constants, interfaces, types, decorators and utilities shared by all services",
  "main": "dist/index.js",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.15",
    "@nestjs/microservices": "^10.4.15",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
